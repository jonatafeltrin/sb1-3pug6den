import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  tag: {
    marginRight: 8,
  },
  contentContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  icon: {
    marginRight: 8,
  },
});
